/**
 * 乾坤全局状态中 actionType 类型
 */
export const enum GlobalStateActionTypes {
    /**
     * 配置 config
     */
    config,
    /**
     * 子应用切换，页面跳转
     */
    route,
    /**
     * 切换语言
     */
    language,
    /**
     * 切换主题
     */
    theme,
}
