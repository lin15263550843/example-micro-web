/**
 * 环境标识
 */
export const enum Env {
    /**
     * 本地环境
     */
    local = 'local',
    /**
     * 开发环境
     */
    dev = 'dev',
    /**
     * 测试环境
     */
    test = 'test',
    /**
     * 生产环境
     */
    prod = 'prod',
}
/**
 * 环境名称
 */
export enum EnvName {
    /**
     * 本地环境
     */
    local = '本地环境',
    /**
     * 开发环境
     */
    dev = '开发环境',
    /**
     * 测试环境
     */
    test = '测试环境',
    /**
     * 生产环境
     */
    prod = '生产环境',
}
