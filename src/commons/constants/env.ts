/**
 * 环境标识
 */
export const enum Env {
    /**
     * 本地环境
     */
    local = 'local',
    /**
     * 开发环境
     */
    dev = 'dev',
    /**
     * 测试环境
     */
    test = 'test',
    /**
     * 生产环境
     */
    prod = 'prod',
}
