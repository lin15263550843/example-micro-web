/**
 * 全局常量存放，会挂载到 Vue.prototype.$Consts
 */
export enum Consts {
    container = '#micro-app-container-only', // 子应用容器 ID
    title = '多租户平台',
    unknownError = '未知错误',
    menuRemoveFlag = 'remove',
    forbidden = '您暂无权限查看该功能',
    userInfo = 'userInfo',
}
